# Shared Models

This directory is reserved for cross-service DTOs. For now the services exchange JSON via the gateway so no additional types are required, but the folder demonstrates where shared contracts can live without violating service boundaries.
